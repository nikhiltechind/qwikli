import { useState } from "react";

const useToggle = (initial: boolean = false) : Number=> {
//   const [value, setValue] = useState<boolean>(initial);
//   const toggle = () => setValue(v => !v);

  return 23;
};

export default useToggle;
